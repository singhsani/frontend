import { FilterAttachmentPipe } from './filter-attachment.pipe';

describe('FilterAttachmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAttachmentPipe();
    expect(pipe).toBeTruthy();
  });
});
