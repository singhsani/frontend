import { MaskRegistrationNumVtDirective } from './mask-registration-num-vt.directive';

describe('MaskRegistrationNumVtDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskRegistrationNumVtDirective();
    expect(directive).toBeTruthy();
  });
});
