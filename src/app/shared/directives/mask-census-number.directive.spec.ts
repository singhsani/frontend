import { MaskCensusNumberDirective } from './mask-census-number.directive';

describe('MaskCensusNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskCensusNumberDirective();
    expect(directive).toBeTruthy();
  });
});
