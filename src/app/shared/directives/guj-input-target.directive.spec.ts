import { GujInputTargetDirective } from './guj-input-target.directive';

describe('GujInputTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new GujInputTargetDirective();
    expect(directive).toBeTruthy();
  });
});
