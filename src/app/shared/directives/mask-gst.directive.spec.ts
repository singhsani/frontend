import { MaskGstDirective } from './mask-gst.directive';

describe('MaskGstDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskGstDirective();
    expect(directive).toBeTruthy();
  });
});
