import { FilterAttachmentMFPipe } from './filter-attachment-mf.pipe';

describe('FilterAttachmentMFPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAttachmentMFPipe();
    expect(pipe).toBeTruthy();
  });
});
