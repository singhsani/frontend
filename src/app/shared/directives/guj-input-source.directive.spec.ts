import { GujInputSourceDirective } from './guj-input-source.directive';

describe('GujInputSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new GujInputSourceDirective();
    expect(directive).toBeTruthy();
  });
});
