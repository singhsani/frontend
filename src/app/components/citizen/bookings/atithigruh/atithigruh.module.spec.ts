import { AtithigruhModule } from './atithigruh.module';

describe('AtithigruhModule', () => {
  let atithigruhModule: AtithigruhModule;

  beforeEach(() => {
    atithigruhModule = new AtithigruhModule();
  });

  it('should create an instance', () => {
    expect(atithigruhModule).toBeTruthy();
  });
});
