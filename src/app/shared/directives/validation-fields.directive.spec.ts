import { ValidationFieldsDirective } from './validation-fields.directive';

describe('ValidationFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
